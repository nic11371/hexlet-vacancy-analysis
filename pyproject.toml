[project]
name = "hexlet-vacancy-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.1",
    "dotenv>=0.9.9",
]

[dependency-groups]
dev = [
    "flake8>=7.2.0",
]
